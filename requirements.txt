# For cPanel "Setup Python App" -> Configuration files -> requirements.txt.
#
# Pinned for Python 3.9, which is the highest the Namecheap cPanel Python
# selector offers on this server (3.6.15 / 3.7.17 / 3.8.20 / 3.9.23). Newer
# interpreters exist at /opt/alt/python31{0,1,2,3} but are not exposed by the
# selector.
#
# Django 4.2 LTS is the last release supporting Python 3.9. It reached
# end-of-life in April 2026, so it receives no further security patches — see
# DEPLOY.md "Known limitations".
django>=4.2,<5.0

# 3.9 support: Pillow dropped it in 12.x, so cap below that.
pillow>=10.0.0,<12.0.0

# QR codes for each location's landing page. Pure Python; renders SVG on its
# own and PNG via Pillow, which is already a dependency.
qrcode>=7.4,<9.0

# Batch QR sheets as printable A4 PDFs. Capped below 4.5 for Python 3.9, and
# imported lazily in qr_pdf.py so a server that has not run pip install yet
# loses the print sheet rather than the whole site.
reportlab>=4.0,<4.5
whitenoise>=6.6,<7.0
python-dotenv>=1.0.0
requests>=2.31.0
# Left unpinned so pip picks the newest build still compatible with 3.9.
anthropic
