# For cPanel "Setup Python App" -> Configuration files -> requirements.txt.
# Local development uses uv + pyproject.toml/uv.lock instead; keep the two in
# sync when you add a dependency.
#
# Deliberately loose: the shared host may only offer Python 3.10/3.11, and hard
# pins from a 3.13 lockfile will fail to resolve there.
#
# Django range is intentional — pip picks by the server's Python version:
#   Python 3.12+  -> Django 6.0.x (what local dev runs)
#   Python 3.10/11 -> Django 5.2.x LTS (fallback; test suite verified against it)
anthropic>=0.102.0
django>=5.2,<6.1
pillow>=10.0.0
python-dotenv>=1.0.0
requests>=2.31.0
whitenoise>=6.8
